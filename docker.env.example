# Rename/copy to ".env" next to docker-compose.yml (Docker Compose loads it automatically).
# Then: docker compose build --no-cache frontend

VITE_STRIPE_PUBLISHABLE_KEY=pk_test_paste_same_as_stripe_publishable_key_in_application_properties
